# notes/

Session / task handoffs. Decisions that change the product go in
`projects/whaletrail/docs/SCOPE.md`; how to run goes in ENVIRONMENT / DEPLOY.
This folder is the pause tape: what was happening, what landed, what is left.

One file per pause, named `YYYY-MM-DD-<short-slug>.md`.
